package ejercicios5Abril

fun main() {
    val texto = "Kotlin"
    val lista = listOf(10, 20, 30, 40, 50)

    println(texto.length)
    println(texto.indices)
    println(lista.indices)
    println(lista.indices.first)
    println(lista.indices.last)
}